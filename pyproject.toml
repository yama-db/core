[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mountain_db_shared"
version = "0.1.0"
dependencies = []

[tool.setuptools]
packages = ["shared"]

